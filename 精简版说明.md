# Oh My OpenCode 精简版

基于 [oh-my-opencode](https://github.com/code-yeongyu/oh-my-opencode) 的精简版本，移除了大量占用上下文的功能，保留核心能力。

## 精简内容

### 已删除的 Hooks（14 个）

| Hook | 原作用 | 删除原因 |
|------|--------|---------|
| `todo-continuation-enforcer` | 强制 AI 继续未完成的 todo | 烦人，自动提醒 |
| `ralph-loop` | 循环执行模式 | 不常用 |
| `delegate-task-retry` | 委托任务失败时注入重试提示 | 不常用 |
| `keyword-detector` | 检测 ultrawork/search/analyze 关键词并注入大段指令 | **严重占用上下文（~5000字符/次）** |
| `agent-usage-reminder` | 每次搜索后提醒"你应该用代理" | **占用上下文（~500字符/次）** |
| `auto-update-checker` | 启动时检查更新 | 不需要 |
| `startup-toast` | 启动时弹窗提示 | 不需要 |
| `directory-readme-injector` | 读文件时自动注入目录的 README.md | **占用上下文，AI 会自己读** |
| `directory-agents-injector` | 读文件时自动注入目录的 AGENTS.md | **占用上下文，AI 会自己读** |
| `rules-injector` | 读文件时自动注入 .rules/.cursorrules 文件 | **占用上下文，AI 会自己读** |
| `edit-error-recovery` | Edit 失败时注入提醒 | AI 自己会处理 |
| `sisyphus-orchestrator` | 编排器模式检测和提醒 | 不用编排器就没用 |
| `prometheus-md-only` | 限制 Prometheus 代理只能写 .md 文件 | 不用规划功能就没用 |
| `start-work` | 支持 `/start-work` 命令 | 不用就没用 |

### 已删除的 CLI 命令

| 命令 | 原作用 |
|------|--------|
| `get-local-version` | 检查本地版本和更新 |

## 保留的核心功能

### 子代理（全部保留）

| 代理 | 作用 | 触发方式 |
|------|------|---------|
| `oracle` | 高级推理、架构咨询 | `delegate_task(agent="oracle")` |
| `explore` | 代码探索、模式查找 | `delegate_task(agent="explore")` |
| `librarian` | 文档查询、GitHub 搜索 | `delegate_task(agent="librarian")` |
| `frontend-ui-ux-engineer` | 前端 UI/UX 设计 | `delegate_task(agent="frontend-ui-ux-engineer")` |
| `document-writer` | 文档撰写 | `delegate_task(agent="document-writer")` |
| `multimodal-looker` | 多模态分析 | `delegate_task(agent="multimodal-looker")` |
| `prometheus` | 规划代理 | 手动选择 |
| `momus` | 计划审核 | 手动选择 |
| `orchestrator-sisyphus` | 编排器 | `/orchestrator-sisyphus` 命令 |

**注意**：子代理提示词不会自动加载到主会话，只有调用时才会加载到独立的子会话中。

### 保留的 Hooks（15 个）

| Hook | 作用 |
|------|------|
| `context-window-monitor` | 监控上下文窗口使用量 |
| `session-recovery` | 会话崩溃自动恢复 |
| `session-notification` | 任务完成通知 |
| `comment-checker` | 阻止 AI 添加过多注释 |
| `grep-output-truncator` | 截断过长的 grep 输出 |
| `tool-output-truncator` | 截断过长的工具输出 |
| `empty-task-response-detector` | 检测空响应 |
| `think-mode` | 思考模式支持 |
| `anthropic-context-window-limit-recovery` | Anthropic 上下文超限恢复 |
| `background-notification` | 后台任务完成通知 |
| `non-interactive-env` | 非交互环境处理 |
| `interactive-bash-session` | tmux 交互终端 |
| `thinking-block-validator` | 验证 thinking block 格式 |
| `compaction-context-injector` | 压缩上下文注入 |
| `claude-code-hooks` | Claude Code 兼容钩子 |
| `auto-slash-command` | 自动斜杠命令 |

### 保留的工具

- `delegate_task` - 委托任务给子代理
- `call_omo_agent` - 调用代理
- 后台任务管理
- LSP 工具（代码分析、跳转定义等）
- AST-grep（代码模式搜索和替换）

## 节省的上下文

| 场景 | 预计节省 |
|------|---------|
| 每次对话 | 10,000 - 30,000 字符 |
| 使用搜索功能 | ~500 字符/次（不再注入提醒） |
| 触发 ultrawork 关键词 | ~5,000 字符（不再注入） |
| 读取文件 | 可变（不再自动注入 README/AGENTS.md/规则） |

## 构建大小对比

| 版本 | 插件大小 |
|------|---------|
| 原始版本 | 1.83 MB |
| 精简版本 | 1.72 MB |
| 减少 | 110 KB |

## 使用方法

### 构建

```bash
cd oh-my-opencode
bun install
bun run build
```

### 配置示例

```json
{
  "$schema": "https://raw.githubusercontent.com/code-yeongyu/oh-my-opencode/master/assets/oh-my-opencode.schema.json",
  "agents": {
    "Sisyphus": {
      "model": "your-model-here"
    },
    "oracle": {
      "model": "your-oracle-model"
    },
    "explore": {
      "model": "your-explore-model"
    },
    "librarian": {
      "model": "your-librarian-model"
    }
  }
}
```

## 注意事项

1. **子代理仍可正常使用** - 只是移除了自动注入的行为，核心调用功能完好
2. **AI 会自己读规则文件** - 移除自动注入后，AI 需要时会主动读取 README/AGENTS.md 等文件
3. **规划功能仍可用** - Prometheus、Momus 等规划代理仍在，只是需要手动调用
4. **编排器仍可用** - orchestrator-sisyphus 仍在，通过 `/orchestrator-sisyphus` 命令调用

## 原项目

- GitHub: https://github.com/code-yeongyu/oh-my-opencode
- 作者: [YeonGyu Kim](https://github.com/code-yeongyu)
